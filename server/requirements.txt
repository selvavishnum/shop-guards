fastapi==0.115.5
uvicorn[standard]==0.32.1
httpx==0.28.0
apscheduler==3.10.4
python-dotenv==1.0.1
Pillow==10.4.0
numpy==1.26.4
opencv-python-headless==4.10.0.84
# ^ if you bump this version, also update the matching pin in render.yaml's
#   buildCommand (the final --force-reinstall step) — they must stay in sync.
ultralytics==8.3.40
python-multipart==0.0.17
